name: Auto Pull Requests test

on:
  push:
    branches:
      - Conflomark_Maven

jobs:
  createPullRequest:
    runs-on: ubuntu-latest
    steps:
      - name: Revisando Código
        uses: actions/checkout@v2

      - name: Extraer Datos de Confirmación de Git
        uses: rlespinasse/git-commit-data-action@v1.x

      - name: Subir Pull Requests
        uses: peter-evans/create-pull-request@v3
        with:
          branch: "sonarqube"
          base: Conflomark_Maven
          title: "Auto Pull Requests test"
          committer: ${{ secrets.GIT_COMMIT_AUTHOR_NAME }} <${{ secrets.GIT_COMMIT_AUTHOR_EMAIL }}">
          author: ${{ secrets.GIT_COMMIT_AUTHOR_NAME }} <${{ secrets.GIT_COMMIT_AUTHOR_EMAIL }}">
          body:
            Creación Automática de Pull Requests test
          token: ${{ secrets.TOKEN_GITHUB }}
          delete-branch: false
