name: Auto Pull Requests

on:
  push:
    branches:
      - Conflomark_Maven

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Revisando Código
        uses: actions/checkout@v3

      - name: Create Pull Request
        run: |
          git config --global user.name "juancondorijara"
          git config --global user.email "juan.condori.jara@vallegrande.edu.pe"
          git checkout sonarqube
          git merge Conflomark_Maven
          git push origin sonarqube
